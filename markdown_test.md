#一级标题
##二级标题
**我变大了吗**
*斜不拉几*
###不对劲
# 这样才对
## 隔一个空格
```javascript
function fancyAlert(arg) {
  if(arg) {
    $.facebox({div:'#foo'})
  }
}
```
  function fancyAlert(arg) if(arg) {
        $.facebox({div:'#foo'})
      }
    }
  function fancyAlert(arg) {
    if(arg) {
      $.facebox({div:'#foo'})
        }
  }
